# Google OAuth client ID — Web application type, no client secret.
# Create at: https://console.cloud.google.com → APIs & Services → Credentials
# Authorized JavaScript origins must include http://localhost:5173 (dev) and the prod URL.
VITE_GOOGLE_CLIENT_ID=
